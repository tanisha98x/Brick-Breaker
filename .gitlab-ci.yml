image: maven

sonar_analysis:
  script:
    - java -version
    - mvn -version
    - mvn -X sonar:sonar -Dsonar.host.url=http://coursework.cs.duke.edu:9200
  tags:
    - sonarqube
  only:
    - master

